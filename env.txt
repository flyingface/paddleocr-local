# --- 修改为适配 RTX 50 系列 (Blackwell/SM120) 的官方镜像标签 ---
# 必须包含 nvidia 前缀以支持最新的 1.5 模型
API_IMAGE_TAG_SUFFIX=latest-nvidia-gpu-sm120-offline

# 指定使用 vllm 后端 (保持不变)
VLM_BACKEND=vllm

# 同样修改 VLM 服务的镜像标签为官方 sm120-offline 版本
VLM_IMAGE_TAG_SUFFIX=latest-nvidia-gpu-sm120-offline
